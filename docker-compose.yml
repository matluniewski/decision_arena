services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: decision_arena
      POSTGRES_USER: decision_arena
      POSTGRES_PASSWORD: decision_arena
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
